%h1 Listing areas

%table
  %tr
    %th Name
    %th Type
    %th Parent
    %th
    %th
    %th

  - @areas.each do |area|
    %tr
      %td= link_to area.name, area
      %td= area.type
      %td= link_to area.parent.name, area.parent unless area.parent.nil?
      %td= link_to 'Edit', edit_area_path(area)
      %td= link_to 'Destroy', area, data: { confirm: 'Are you sure?' }, method: :delete

= paginate @areas

%br

= link_to 'New Area', new_area_path
